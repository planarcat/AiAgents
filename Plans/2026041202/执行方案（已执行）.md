# 执行方案（已执行）· 批次 2026041202

> **执行状态**：**已执行**  
> **结案日期**：2026-04-12  
> **依据**：[03-Agent-Skills范式-能力系统再设计.md](03-Agent-Skills范式-能力系统再设计.md)、[04-示例Skill-天气查询.md](04-示例Skill-天气查询.md)、[01-Agent-Skills架构设计.md](01-Agent-Skills架构设计.md)  
> **后续未结项**：见同目录 **[未执行方案.md](未执行方案.md)**（跨批次累积）。

---

## 1. 范围与目标（回顾）

| 目标 | 说明 |
|------|------|
| **L1/L2 数据** | `skills` 表增加 `instructions_md`、`kind`；天气种子写入示例 L2（见 `04`）。 |
| **运行时** | 当轮 system：**base → 内置 runtime → L2 → 能力清单后缀**（[03 §5.1](03-Agent-Skills范式-能力系统再设计.md)）。L2 **不**写入 `messages`。 |
| **执行层** | 内置工具逻辑抽到 `hub/skill_dispatch.rs`。 |
| **目录 API** | `skills_catalog` 增加 `kind`；前端 `SkillCatalogItem.kind`。 |

**本批次明确不做**：前端编辑 L2、MCP、`anthropic_remote`、会话侧常驻能力摘要 UI。

---

## 2. 里程碑与验收（回顾）

| # | 内容 | 验收 |
|---|------|------|
| M1 | 迁移 SQL 落地 | 新库/升级库含 `instructions_md`、`kind`；天气行含 L2 |
| M2 | `messages_send` 注入 L2 | 绑定含天气 Skill 时，当轮 system 含「能力指导」块 |
| M3 | `skill_dispatch` + 瘦 `dispatch_tool` | 天气/委派行为与迁移前一致 |
| M4 | `skills_catalog` + 前端 `kind` | `cargo check`、`pnpm exec tsc -b` 通过 |

---

## 3. 已执行结果（落地对照）

| 交付物 | 路径或说明 |
|--------|------------|
| 迁移 | `Apps/agent-hub/src-tauri/migrations/20260412230000_skills_instructions_kind.sql` |
| L2 注入 | `Apps/agent-hub/src-tauri/src/hub/chat.rs`：`load_skill_instructions_suffix`；`SKILL_INSTRUCTIONS_MAX_CHARS` |
| 工具分发 | `Apps/agent-hub/src-tauri/src/hub/skill_dispatch.rs`：`invoke_builtin`；`hub/mod.rs` 注册模块 |
| 目录 API | `Apps/agent-hub/src-tauri/src/hub/skills.rs`：`SkillCatalogItem.kind` |
| 前端 | `Apps/agent-hub/src/App.tsx`、`AssistantConfigPanel.tsx`：`kind` 字段与归一化 |

---

## 4. 技术要点（回顾）

- L2 单段截断：4000 Unicode 标量（与 `truncate_chars_for_system` 一致思路）。  
- 绑定查询：`skills` JOIN `agent_skills`；空 `instructions_md` 跳过。  
- L2 拼接顺序：未排序（与 [03 §8.1](03-Agent-Skills范式-能力系统再设计.md) 一致）。

---

## 5. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-12 | 初稿：`执行方案.md`。 |
| 2026-04-12 | 结案：落地后补 **§3 已执行结果**；文件名改为 **`执行方案（已执行）.md`**；与 `未执行方案.md` 配对。 |
