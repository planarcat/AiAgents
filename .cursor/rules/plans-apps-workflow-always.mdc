---
description: Plans/Apps 工作流与「仅方案设计」约束；在 Agent Skills 未自动加载时仍生效。完整批次与文件约定见 .cursor/skills/plans-apps-workflow/SKILL.md。
alwaysApply: true
---

# Plans / Apps 与方案讨论（仓库级约束）

本规则与 `.cursor/skills/plans-apps-workflow/SKILL.md` **一致**；若 Skills 未进入上下文，**仍须**遵守本节。

## 目录

- **`Plans/`**：讨论方案、决议与修订；**不经用户要求**不在此写「可执行代码实现」。
- **`Apps/`**：正式开发与可运行工程；以已确认的 Plans / 执行方案为依据推进。

## 「仅方案 / 讨论设计方案」用语触发时

当用户使用类似表述**框定当前回合为设计讨论**时（示例：进行方案设计、讨论设计方案、讨论方案、仅讨论方案、先讨论方案、设计阶段、先把方案说清楚、只谈架构不写代码、接下来讨论设计方案；或英文 *design discussion only*、*let's discuss the approach first*），助手**必须**：

1. **默认不改动 `Apps/`**：不新增/修改业务逻辑，不仅为「演示方案」改代码；不主动跑 `cargo` / `pnpm` 作实现验证，除非用户**同时**要求实现或验证。
2. **交付物限于设计层**：目标、约束、架构与流程、数据/调用关系、利弊、风险、分期、待决项；需要时仅用伪代码或文件职责说明，**不直接提交实现补丁**。
3. **「怎么做」≠「现在就改代码」**：先给文字方案；若建议落地，**先询问**是否按此修改 `Apps/`，**未获同意不改仓库**。
4. **`Plans/` 落盘**：用户希望留档时再按 `SKILL.md` 的批次与命名写入；**仅讨论不落盘**合法。
5. **同一话题下的连续追问**（含「这个怎么做」）**仍视为设计讨论**，除非用户明确说实现 / 落地 / 改某文件。

**结束仅设计模式**（任一即可）：用户明确说实现、开发、落地、按方案写代码、改某路径、可以动手了、在 Apps 里实现等。

**先方案后开发**：在 `Plans/` 落盘或用户确认方案后，再在 `Apps/` 实现（除非用户明确只要小修且声明不写 Plan）。若已用「仅设计」用语框定回合，**在对方明确允许实现前不得改 `Apps/`**。

## 批次与执行方案

新开批次、`执行方案.md`、结案与 `执行方案（已执行）.md` 等规则以 **`SKILL.md` 全文**为准；涉及记方案、综合执行方案、批次归类时，**先读** `.cursor/skills/plans-apps-workflow/SKILL.md` 再落盘。

## 执行方案结案与未执行方案累积（摘要）

- **`执行方案` 在 `Apps/` 执行完毕后**：**必须**标记 **已执行**（改名或文首状态），并产出 **已执行结果** + **未执行方案**（待执行 / 待设计 / 讨论未执行 / 执行失败等；排除本批已执行项与讨论中剔除项）。
- **未执行方案跨批次累积**：新一批的未执行清单 = 上一批累积 **减去** 本批已执行 **减去** 明确弃用；推荐 `未执行方案.md` 或写在 `执行方案（已执行）.md` 内。
- **新开批次第一版设计稿**（如 `01-….md`）：**须先**引入上一批 **未执行项**（引用或摘录），再写新议题；不得默认从零开始（除非用户声明整批弃用历史未执行项）。

完整条文见 `.cursor/skills/plans-apps-workflow/SKILL.md` 中 **「执行方案落地后的产出与未执行方案累积」**。
